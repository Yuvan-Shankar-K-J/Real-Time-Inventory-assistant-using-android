import 'package:flutter/material.dart';

class BillSection extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Bill Section")),
      body: Center(child: Text("Generate and view bills")),
    );
  }
}
